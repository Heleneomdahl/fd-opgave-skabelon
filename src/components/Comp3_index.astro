<section>
  <article>
    <div>
      <h4>EXPERIENCE</h4>
      <h2>More than 25 years of experience</h2>
      <p>3000+ our clients are insured around the World</p>
    </div>
    <div class="stats">
      <div>
        <div class="stat" style="--percent: 85"></div>
        <h4>Consistency</h4>
      </div>
      <div>
        <div class="stat" style="--percent: 95"></div>
        <h4>Improvement</h4>
      </div>
      <div>
        <div class="stat" style="--percent: 90"></div>
        <h4>Branching</h4>
      </div>
    </div>
  </article>
</section>

<style>
  section {
    grid-column: full;
    display: grid;
    grid-template-columns: subgrid;
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-3xl);
    background-color: var(--accent-color);

    > * {
      grid-column: content;
    }
    article:has(div) {
      display: flex;
      justify-content: space-between;
    }
    .stats {
      display: flex;
      gap: 3rem;
      width: min(600px, 100% - 1rem);

      > div:has(div, h4) {
      }
      text-align: center;
      font-weight: 600;
    }
  }

  .stat {
    container: stat / inline-size; /* gør at man kan putte en font-size på &after:: */
    --actualPercent: calc(var(--percent) * 1%);
    --border-width: 5px;
    aspect-ratio: 1;
    width: 150px;
    border-radius: calc(Infinity * 1px);
    border: var(--border-width) solid transparent;
    background:
      conic-gradient(var(--accent-color)) padding-box,
      conic-gradient(var(--primary-color) var(--actualPercent), rgb(203, 178, 37) 0) border-box; /* border-box gør at den fylder farven på gradienten ud, hvis man ikke vil have en farve på border*/ /* dette gør at farven kun ses som omrids af cirklen */

    display: grid;
    counter-reset: c var(--percent);
    margin-bottom: 1rem;

    &::before,
    &::after {
      grid-area: 1 / 1;
    }

    &::after {
      content: counter(c) "%";
      place-self: center;
      font-size: 30cqw;
      font-variant-numeric: tabular-nums; /*tallet ikke ændre størrelse alt efter antallet af cifre, så 1 og 30 har ca samme placering uden at tallet hopper for meget rundt*/
    }

    &::before {
      content: "";
      aspect-ratio: 1;
      border-radius: 99px;
      border: 3px solid var(--primary-color);
      width: 20px;
      background: var(--secondary-color);
      offset-path: border-box;
      offset-anchor: 50% calc(50% - var(--border-width) / 2); /*gør at prikken placerer sig centralt på banen*/
      offset-distance: var(--actualPercent);
    }
  }
</style>
