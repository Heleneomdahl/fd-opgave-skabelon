---

---

<section>
  <div>
    <a href="/">AskExperts</a>
  </div>
  <div>
    <a href="/">Home</a>
    <a href="/case">Case Study</a>
    <a href="/team">Team</a>
    <a href="/about">About</a>
  </div>
  <div>
    <button popovertarget="popup">Login</button>
    <div id="popup" popover="auto">
      <h2>Dashboard Login</h2>
      <form action="/action_page.php">
        <label for="email">Email address</label><br />
        <input type="email" id="email" name="email" value="Type..." /><br />
        <label for="Password">Password</label><br />
        <input type="password" id="password" name="password" value="Type..." /><br /><br />
        <input type="submit" value="Go to dashboard" />
      </form>
    </div>
  </div>
</section>

<style>
  @layer components {
    section {
      grid-column: full;
      display: flex;
      justify-content: space-around;
      align-items: center;
      padding: var(--space-s);
      background-color: var(--header-bg);
      box-shadow: var(--shadow-md);
      background-color: var(--hero-card-bg);

      a {
        text-decoration: none;
        color: var(--text-primary);
        font-weight: 500;
        margin-right: var(--space-lg);
        color: var(--primary-color);

        &:hover {
          color: var(--primary-600);
        }
      }
      > div:last-child {
        background-color: var(--accent-color);
        padding: var(--space-3xs);
        border-radius: 20px;
      }
      #popup::backdrop {
        backdrop-filter: blur(5px);
      }
    }
  }
</style>
